org.gradle.jvmargs=-Xmx8G -XX:MaxMetaspaceSize=4G -XX:ReservedCodeCacheSize=512m -XX:+HeapDumpOnOutOfMemoryError
android.useAndroidX=true
# Kotlin comes from the Flutter/Android Gradle plugins rather than from the
# Kotlin Gradle Plugin applied by hand. AGP 9 dropped KGP support, and Flutter
# only carries a temporary shim for plugins that still apply it.
android.builtInKotlin=true
# This newDsl flag was added automatically by Flutter migrator
android.newDsl=false
